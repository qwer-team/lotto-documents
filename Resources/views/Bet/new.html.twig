{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bet creation</h1>

    <form action="{{ path('bet_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('bet') }}">
            Back to the list
        </a>
    </li>
</ul>
    
  <form action="{{ path('bet_request') }}" method="post" {{ form_enctype(raw_bet_form) }}>
        {{ form_widget(raw_bet_form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>  
{% endblock %}
